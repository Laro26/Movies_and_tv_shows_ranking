import { Component, Input, ChangeDetectionStrategy } from '@angular/core';
//import {Movies} from './componentes/Inerface/movies.interface';
@Component({
    selector:'app-series',
    template:'',
})
export class SeriesComponent{
   // @Input() movies:Movies;
} 